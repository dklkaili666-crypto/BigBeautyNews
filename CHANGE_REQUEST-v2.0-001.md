# CR-v2.0-001：Flash 翻译长度纠偏重试

- 状态：Approved
- 批准人：项目所有者
- 批准证据：2026-08-23 明确回复“批准 CR-v2.0-001”
- 提出日期：2026-08-23
- 基线：已批准 PRD v2.0、已批准实施计划 v2.0

## 请求变更

当 AI 或政经翻译结果违反现有标题/摘要长度规则时，第二次尝试应向 Flash 提供仅包含违规 rank、实际长度和既有目标区间的纠偏说明，要求重新输出完整 JSON。不得记录或回传第一轮的原始标题、摘要或思考内容。

同时将现有通用错误“翻译结果不满足标题或摘要长度要求”细化为安全的长度元数据，例如：`rank=2 title_length=54 summary_length=43`，便于确认失败原因。

现有质量规则保持不变：标题不超过50字；摘要目标100～200字，可接受范围50～500字。

## 原因与证据

[生产运行 32639343904](https://github.com/dklkaili666-crypto/BigBeautyNews/actions/runs/32639343904) 已证明：

- `deepseek-v4-flash` 模型名有效；
- `thinking=disabled` 和 `response_format=json_object` 均被当前 API 接受；
- AI 与政经排序成功，`reasoning_length=0`，返回合法 JSON；
- AI 翻译两次均返回 HTTP 200、合法 JSON，但违反现有标题或摘要长度规则；
- 流水线按设计在推送前退出，没有发送不完整微信内容。

PRD v2.0 明确禁止修改翻译提示词，因此不能在原范围内增加纠偏提示。

## 受影响需求

- FR-019：四处调用使用非思考 JSON 输出。
- FR-020：失败处理和重试。
- FR-022：真实端到端恢复验收。
- NFR-013：不得记录模型原文或思考内容。
- NFR-014：既有标题与摘要质量规则保持不变。

建议新增：

- FR-023：翻译长度纠偏重试。

## 范围与设计影响

- 修改 `src/pipeline/translator.py`：第一次长度校验失败后，为第二次请求追加结构化长度纠偏说明。
- 修改 `tests/test_llm_pipeline.py` 与 `tests/test_geopolitics_llm.py`：覆盖 AI/政经翻译的纠偏请求、成功恢复、两次失败及日志脱敏。
- 更新 PRD、实施计划与验收记录为 v2.1。
- 不修改标题/摘要长度阈值、基础翻译提示词、模型、API Base/Key、排序、调度、推送或回退策略。

## 测试与交付影响

- 新增测试：第一次长度不合格、第二次合格时成功返回。
- 新增测试：纠偏说明只包含 rank 与长度，不包含第一轮模型原文。
- 保留两次均不合格时失败、不推送的现有行为。
- 重新运行全量检查和真实非强制工作流验收。

## 已完成工作影响

- T20-001～T20-004 保持有效，无需撤销。
- `LLM_MODEL` 已切换为 `deepseek-v4-flash`，Base/Key 未变。
- T20-005 处于阻塞状态；批准本变更后恢复。

## 推荐方案

批准本变更。它不降低质量标准，只让第二次机会针对实际违规项纠偏，是恢复 Flash 稳定翻译的最小改动。

## 备选方案

1. 原样再次运行：可能偶然成功，但没有解决 Flash 对长度约束执行不稳定的问题，不推荐。
2. 放宽摘要或标题长度：实现简单，但会降低既有内容质量标准，不推荐。
3. 自动回退 Pro：违反已批准的不回退决策，并重新引入 Pro 兼容问题，不推荐。

## 版本建议

- PRD：v2.0 → v2.1。
- 实施计划：增加 T21-001 翻译纠偏、T21-002 回归与重新发布、T21-003 真实验收。
