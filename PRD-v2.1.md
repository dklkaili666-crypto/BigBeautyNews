# BigBeautyNews v2.1 PRD：Flash 翻译长度纠偏

- 状态：Approved
- 批准人：项目所有者
- 批准证据：2026-08-23 明确回复“批准 PRD v2.1”
- 基线：已批准 PRD v2.0
- 变更来源：已批准 CR-v2.0-001
- 提出日期：2026-08-23
- 目标用户：项目所有者

## 背景

PRD v2.0 的代码、测试和生产模型切换已完成。[生产运行 32639343904](https://github.com/dklkaili666-crypto/BigBeautyNews/actions/runs/32639343904) 证明 `deepseek-v4-flash` 支持非思考 JSON 调用，AI 与政经排序成功；但 AI 翻译两次返回的合法 JSON 均有标题或摘要违反既有长度规则，流水线按设计在微信推送前终止。

本版本只增强第二次翻译尝试的纠偏信息，不降低既有质量标准。

## 目标与成功标准

当 Flash 第一次翻译产生长度不合格结果时，让第二次尝试明确知道哪些 rank 违规以及实际长度，从而在不放宽标题/摘要标准的前提下完成 AI 5 条与政经5条翻译和微信推送。

成功标准：

- AI 与政经翻译均支持一次针对长度违规的纠偏重试。
- 第二次请求只新增违规 rank、字段类型、实际长度和既有目标区间，不包含第一次模型生成的标题、摘要或思考内容。
- 标题不超过50字；摘要目标100～200字、可接受范围50～500字，标准保持不变。
- 一次真实非强制工作流成功生成 AI 5 条与政经5条并只发送一条 Server酱消息。

## 用户与场景

### 场景 S-004：Flash 翻译长度纠偏

第一次翻译结果中如有标题超过50字，或摘要少于50字/超过500字，系统收集安全的长度元数据，并在第二次请求中要求 Flash 重新输出完整5条 JSON；合格后继续流水线。

### 场景 S-005：纠偏后仍不合格

第二次翻译结果仍不合格时，本次运行失败，不持久化成功日报、不调用 Server酱、不写成功推送日期；08:15 兜底保持现有行为。

## 范围

### 范围内

- 修改 AI 与政经翻译的长度校验，使其生成不含原文的违规元数据。
- 第一次长度校验失败后，为第二次翻译请求追加结构化纠偏说明。
- AI 与政经翻译都必须返回完整5条结果，不接受仅返回违规项。
- 增加纠偏成功、纠偏失败和信息脱敏测试。
- 完成全量验证、重新发布和真实微信推送验收。

### 范围外

- 不修改基础 AI/政经翻译提示词及术语表。
- 不修改标题和摘要长度阈值，不截断、填充或由代码改写模型内容。
- 不增加第三次尝试，不自动回退 Pro 或其他模型。
- 不修改排序、新闻源、筛选、去重、调度、Schema、Server酱格式、API Base/Key 或 `LLM_MODEL`。
- 不处理 CR-001 Server酱 `readkey` 脱敏。

## 功能需求

### FR-023：翻译长度纠偏重试

AI 或政经翻译的第一次响应违反长度规则时，系统必须为第二次请求追加安全、结构化的纠偏说明，并要求重新输出完整 JSON。

验收标准：

- 违规元数据逐项包含 `rank`、违规字段、实际长度和允许范围。
- 第二次请求包含全部既有输入文章，并明确要求返回完整5条结果。
- 第二次请求不得包含第一次模型生成的标题、摘要、原始响应或 `reasoning_content`。
- 第一次不合格、第二次合格时，翻译函数成功返回5条，并保留原文章 URL、来源和其他元数据。
- AI 与政经翻译分别有独立测试证据。

### FR-020（v2.1 修订）：可诊断重试与失败

在 v2.0 的空内容和非法 JSON处理基础上，将标题/摘要长度违规纳入明确诊断和两次尝试机制。

验收标准：

- 长度错误日志只包含 rank、字段和长度，不包含模型生成文本。
- 第二次仍不合格时抛出既有翻译调用失败错误。
- 整个流水线不持久化成功结果、不推送、不写成功日期。

### FR-022（v2.1 续验）：真实端到端恢复

完成纠偏实现后，重新执行 PRD v2.0 的真实端到端验收。

验收标准：

- 工作流在现有15分钟超时内完成。
- AI 5条与政经5条、内部/外部 Schema、Server酱单次推送和数据提交全部成功。
- 运行状态记录模型为 `deepseek-v4-flash`，并满足 `generated=true`、`pushed=true`、`committed=true`、`schemaValid=true`。

## 非功能需求

### NFR-013（v2.1 续验）：响应安全

纠偏逻辑和日志不得泄露第一次模型回答、思考内容或任何 Secret。

验收标准：

- 测试使用唯一敏感标记模拟模型文本，并证明日志与第二次纠偏说明中不存在这些标记。
- 仓库和生产日志不存在 API Key、SendKey 或原始模型响应。

### NFR-014（v2.1 续验）：范围兼容

除长度纠偏重试外，PRD v2.0 的产品行为保持不变。

验收标准：

- 全量测试和静态检查通过。
- 差异中不存在长度阈值、基础提示词、调度、Schema、新闻规则或回退策略改动。

## 假设与依赖

- 当前 Flash 非思考 JSON 配置保持有效；生产运行已验证该依赖。
- Flash 在获得明确长度反馈后能够在第二次尝试中生成合格完整 JSON。
- 如果两次仍持续失败，本版本保持失败和08:15兜底语义，不新增第三种生成路径。

## 风险

- Flash 可能在第二次仍违反长度要求；系统会继续安全失败，不能保证上游模型绝不出错。
- 纠偏说明增加少量输入 token，但远低于重新启用思考模式或回退 Pro 的成本。

## 未决决策

无。CR-v2.0-001 已批准推荐方案；质量阈值、两次上限和不回退策略均保持不变。

## PRD 完整性检查

- S-004 由 FR-023、FR-020 和 NFR-013 覆盖。
- S-005 由 FR-020、FR-022 和 NFR-014 覆盖。
- 每项新增或修订需求均有可观察验收标准。
- v2.1 与 v2.0 的范围和非目标无冲突；无隐藏未决产品决策。

## 修订记录

| 版本 | 日期 | 状态 | 摘要 |
|---|---|---|---|
| v2.1-draft | 2026-08-23 | Draft | 根据已批准 CR-v2.0-001，增加 Flash 翻译长度纠偏重试，不放宽质量标准。 |
| v2.1 | 2026-08-23 | Approved | 项目所有者批准长度纠偏范围、质量标准和验收条件。 |
